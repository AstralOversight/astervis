version: "3.9"
services:
  web:
    image: ghcr.io/astraloversight/astervis:latest
    ports:
      - "8000:8000"
    command: /bin/bash entry-point.sh
    volumes:
      - astervis:/code
  #   depends_on:
  #     - db
  # db:
  #   image: docker.io/postgres
  #   volumes:
  #     - postgres-data:/var/lib/postgresql
  #   environment:
  #     - "POSTGRES_HOST_AUTH_METHOD=trust"

volumes:
  # postgres-data:
  astervis: